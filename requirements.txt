# ============================================================
# Rebuilt trading agent (agent/) dependencies
# ============================================================

# Core (required to run backtests, paper trading, tests)
numpy>=1.23.0
pandas>=2.0.0
requests>=2.28.0

# Live trading on Webull (official OpenAPI SDK).
# Only needed for `agent.cli live`. Install separately if going live:
#   pip install webull-python-sdk-core webull-python-sdk-trade
# webull-python-sdk-core>=0.1.18
# webull-python-sdk-trade>=0.1.18

# Dev / tests
pytest>=7.0.0

# Optional: plotting and extra analytics
# matplotlib>=3.7.0
# scipy>=1.10.0
